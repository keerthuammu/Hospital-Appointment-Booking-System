# 📋 Team Member Roster & Tasks

Each member should follow these **short instructions** to ensure the project move fast:

| Member | Role | Short Goal |
| :--- | :--- | :--- |
| **Keerthana** | Leader | Review PRs & merge them into `develop`. |
| **Milan** | UI | Edit `:root` in `css/style.css` for the "Perfect UI". |
| **Jidhin** | HTML | Clean up semantic tags in `index.html`. |
| **Liya** | CSS | Fix mobile padding/margins in `css/style.css`. |
| **Jasir** | Bootstrap | Replace custom modals with "Bootstrap Modals". |
| **Krishna**| JS | Improve date-validation logic in `js/script.js`. |
| **Joseph** | jQuery | Add "Smooth Scroll" & "Click Events" in `js/script.js`. |
| **Josekutty**| AJAX | Swap `getJSON` for `ajax()` calls to PHP files. |
| **Jobin** | PHP | Write SQL logic in `backend/api/book_appointment.php`. |
| **Lamees** | DB | Add unique constraints to `database/schema.sql`. |
| **Mahi** | Integrate | Ensure `js` data matches `php` variables. |
| **Jiya** | QA | Log all bugs in `TESTING.md`. |

---

## 🛠️ Management Essentials
- **Master CSS File**: `css/style.css`
- **Master JS File**: `js/script.js`
- **Backend Entry**: `backend/includes/config.php`

## 🌿 Git Management (Clear Commands)
- **Start Day**: `git pull origin develop`
- **Work**: `git checkout <your-branch>`
- **Finish Day**: `git add .` -> `git commit -m "Done"` -> `git push origin <your-branch>`
